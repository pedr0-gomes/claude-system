# CONTEXT.md — .claude/ (sistema de orquestração)

Última atualização: 2026-06-20

## O que é este diretório

`.claude/` é o sistema vivo onde Pedro orquestra como ele constrói coisas comigo. Skills, memória, sessões, configurações. Cada projeto em que trabalhamos pode ter seu próprio `.claude/` local — este aqui é o global.

**Como vive (2026-06-03):** a camada curada mora no repo `~/claude-system` (fonte única); o `~/.claude/` que o Claude Code lê **aponta** pra ela via symlinks. Editar no repo = global atualiza sozinho; `git commit` = versionado. Exceção: `settings.json` é cópia (recebe escritas de UI). Procedimento canônico no README do repo.

Fonte canônica gorda (para Pedro navegar): página **"Sistema de gestão"** no Notion, com o database **Construções**.

## Princípio operacional

**Notion gordo, Claude magro.** Documentação canônica vive no Notion. Bruto vive na transcrição local. Claude carrega só o mínimo para performar a sessão atual e estar disponível para a próxima.

## Gênese de projeto (topologia) *(2026-06-12)*

Peça nova do mapa, **distinta dos blocos e do navegador**: descreve como um projeto **nasce**, não como se navega um que já existe. O navegador pressupõe estado local e roteia *dentro* dele; a gênese **cria** esse estado.

Todo projeto começa no hub `claude-system`: Pedro traz a ideia → iteramos e decidimos o escopo → Claude monta o **scaffold mínimo** (CONTEXT.md + CLAUDE.md locais) e abre a entrada no database **Construções**. Daí Pedro vai trabalhar dentro do repo do projeto.

O hub se mantém *current* pela **Construção no Notion + destilar repetido**, **não** pelos docs locais do projeto (que divergem de propósito). Aqui mora um desafio aberto não resolvido — manter a sincronia (a) entre sessões rodando dentro do projeto e (b) entre o projeto e o hub. Ver Frentes abertas: "Sincronia hub↔projeto↔sessões".

## Método de gerência: blocos componíveis

Substitui o antigo "fluxo de aprendizado" de 5 etapas sequenciais. **Não é fluxo** (ordem rígida) — é um conjunto de **blocos autônomos** que Pedro invoca em qualquer ordem. A sequência não existe no mapa; ela emerge na hora, da combinação de "que artefato já existe" + "o que Pedro escolhe chamar". Cada problema segue uma composição diferente — é isso que dá autonomia pra explorar possibilidades.

**O que conecta os blocos não é ordem, são artefatos.** Cada bloco tem um contrato `consome → produz`; a saída de um pluga na entrada de outro. É o contrato nas bordas que garante a composabilidade e impede o fluxo de "quebrar".

**Ambiente** (roda sozinho, Pedro não chama):

- **Capturar** → transcrição bruta contínua (`.claude/sessions/`, `history.jsonl`) + marcadores ("marca isso") **+ captura ativa no fim de sessão** *(emenda implementada 2026-06-12)*: hook `SessionEnd` (`hooks/capturar-feedback-metodo.sh`) roda um Claude headless sobre a transcrição e, se achar **candidatos de feedback-de-método**, escreve nota em `~/claude-system/inbox/` — inverte o antigo "não captura ativamente" (motivo: diagnóstico "nada sinalizou", ver frente "Sincronia"). Ativação por máquina: mesclar o bloco `hooks` do `settings.json` no ativo (README "3b").

**Blocos** (Pedro invoca):

| Bloco | Consome | Produz | Papéis |
|---|---|---|---|
| **Investigar** | "quero entender X" | artefato de pesquisa (interface p/ Aprender e Construir) | co-pilotagem: Pedro pesquisa, Claude guia sem entregar pronto |
| **Aprender** | dúvida just-in-time **ou** `research.md` (do Investigar) | capacidade interna (+ mapa-de-aprendizado) | Pedro consolida, Claude faz andaime |
| **Construir** | ideia/problema (**ou** `research.md` do Investigar) | código **validado** (+ PRD e Kanban intermediários) | mão de Pedro ou de Claude |
| **Destilar** | transcrição bruta + docs | docs internos (destino = parâmetro: projeto ou transversal) | Pedro cura, Claude propõe + escreve; skill `destilar-projeto` cobre o destino "projeto" |
| **Expor** | destilado interno (âncora) + transcrição/marcadores (jornada) | output público (blog) | motor: Pedro escreve, Claude faz andaime (prosa substituível) + crítica; curadorias: Claude propõe, Pedro cura |

**Implementação:** cada bloco vira um *plugin* (agrupando skills) só quando tiver ≥2 skills com o mesmo contrato; até lá fica como skill única ou só como contrato. O contrato é o que agrupa as skills de um bloco.

**Regra de evolução** (marcada 2026-05-29): daqui pra frente, só *preencher* blocos (definir saídas em aberto, adicionar skills) ou *criar novos* — sempre sem quebrar os contratos já fixados.

**Esteira interna da Construir.** O bloco Construir tem estrutura sequencial por dentro — isso **não** contradiz a linha 17 (*"Não é fluxo"*), que vale no *topo*: os blocos compõem sem ordem fixa; um bloco pode ter esteira interna. Escalas diferentes.

`Idea → (opc) Research [delega ao Investigar → research.md] → (opc) Prototype → PRD → Kanban → Implementation → QA/Code Review`

Bordas-artefato: **PRD** (destino + user stories + notas), **Kanban** (PRD em tickets com relações de bloqueio), **código validado** (saída). Revisão é parte da construção: construir não garante correção, então o QA fecha o loop. Research é ponto de *delegação* ao bloco Investigar, não posse — preserva o Investigar como bloco autônomo. Referência: workshop "AI Engineer" do mattpocock (esteira de onde vieram os artefatos; importamos os artefatos, não as setas).

**Esteira interna do Expor.** *(desenhado 2026-05-29)* Mesma lógica de escala da Construir — esteira interna não contradiz o "não é fluxo" do topo. Pedro invoca. Estrutura: **um motor + duas curadorias.** O motor (*escrever pra pensar/registrar*) gira e gera volume; **audiência** e **portfólio** são curadorias *a jusante* — consequência de publicar com consistência e seleção retrospectiva —, não trilhas paralelas (servir os três de cara inflaria o bloco e mataria a frequência). Matéria-prima: o destilado é a **âncora** (tema/decisão), a transcrição + marcadores "marca isso" dão a **jornada** (o como/por quê que o destilado descarta de propósito); ampliar a entrada com a transcrição adiciona fonte sem mudar a saída — não quebra o contrato.

`Dossiê → Arco → Escrita+crítica → post publicado`

Movimentos: **Dossiê** (*Claude* monta a matéria bruta de um tema a partir dos marcadores + destilado; Pedro escolhe/aprova o tema — e o Claude **apresenta a evidência do destilado mas pergunta o encadeamento/causa, não deduz**: o destilado arruma a ordem real, a jornada verdadeira é do Pedro) → **Arco** (*Claude propõe*, Pedro aprova o esqueleto narrativo tensão→virada→lição) → **Escrita+crítica** (a voz é do Pedro; o *Claude entrega a primeira prosa como andaime substituível* pra tirar o Pedro da página em branco, ele reescreve na voz dele, e o Claude então **critica sem reescrever** as versões dele — preservando o *pensar escrevendo*). *Ajuste 2026-05-30: o contrato original "Claude não entrega prosa" não sobreviveu ao primeiro contato — a página em branco é o gargalo, o andaime destrava sem sequestrar a voz.* Bordas-artefato: **dossiê → arco → post**. *Os artefatos vivem no Notion (sob "Sistema de gestão → Expor"), nunca no repo de código — o raciocínio expositivo é privado; só o post final e o código vão a público (fixado 2026-05-30 ao mover o 1º post pra fora do repo; a futura skill deve escrever no Notion, não em `docs/expor/`).* A jusante, sobre o corpo de posts já publicados (não por-post): **destaque** (audiência — Claude sugere, Pedro decide) e **seleção** (portfólio — Claude propõe os melhores, Pedro cura). Fora do desenho: plataforma do blog e virar skill (ver Frentes abertas).

**Esteira interna do Aprender (realização NotebookLM).** *(desenhado 2026-05-30; refinado 2026-06-03 a partir de feedback re-alimentado — a 1ª rodada foi apagada na migração antes de destilar)* O **Aprender é um bloco-sink**: o output real é intangível (capacidade interna); precisa deixar **alguma sombra inspecionável** — senão não se distingue "aprendi" de "senti que aprendi", e não sobra âncora pro Expor. Não obriga próximo bloco; opcionalmente vira âncora do Expor.

`fontes (Pedro+Claude) → diagnóstico do formato (Claude grelha) → instruções (Claude, Pedro aprova) → NotebookLM entrega material → Pedro itera e consolida nota (= mapa, com camada de evidência) → Claude extrai se o Expor pedir`

Movimentos: **seleção de fontes** (co-pilotagem — alimenta o NotebookLM *e* dá contexto ao Claude pra instruir bem) → **diagnóstico do formato** *(2026-06-03)* (Claude **grelha** Pedro pra decidir qual *tipo* de material o NotebookLM gera agora pra esse assunto — não assume o formato) → **instruções** (Claude traduz a vontade em prompt; **Pedro aprova antes de ir**, senão o Claude molda em silêncio *o que* se aprende). Duas camadas de customização do NotebookLM: **por-artefato** (confirmado — `generate report` tem tipos `briefing-doc`/`study-guide`/`blog-post`/`custom`, e todo `generate` aceita descrição em linguagem natural) e **no notebook inteiro** (persona/comportamento — *a verificar*, comando ainda não localizado) → **material + iteração** (NotebookLM entrega; Pedro **itera** tirando dúvidas via `notebooklm ask`, que continua a conversa) → **consolidação** (Pedro fecha uma **nota-compêndio** dentro do NotebookLM via `ask --save-as-note`).

**Camada de evidência na nota** *(desenhado 2026-06-09, extraído da skill `teach` do mattpocock)* — a nota-compêndio descritiva é *coverage*, não evidência; sozinha não distingue "aprendi" de "senti que aprendi" (o problema declarado do bloco-sink). Conserto, **dentro da nota**: Pedro **comprime os conceitos-chave nas próprias palavras** (definições tight, estilo glossário) — comprimir certo *é* a evidência (régua do teach: *"coverage is not learning"*). Espelha o que o Excalidraw já fazia **só pro relacional** (desenhar a hierarquia certa prova entendimento); a camada fecha a simetria pro material em prosa. O **Claude grelha a evidência contra as fontes, não contra o conhecimento paramétrico dele** — senão vira autoridade do tema e alucina correção, quebrando "o conteúdo nunca é do Claude"; espelha o grelho-contra-bitola do `investigar` (lá a bitola é o contexto, aqui são as fontes). Consequência load-bearing: **as fontes do passo 1 precisam sobreviver acessíveis até o fim** da esteira, não só serem consumidas pelo NotebookLM (é o RESOURCES do teach entrando pela porta dos fundos — sem fonte rastreável não há contra o quê grelhar). Adiados conscientemente do teach: **MISSION-compasso** (peso de currículo longo; nosso sink é situado/task-scoped, não currículo), **Wisdom/comunidades** (outra escala), **learning-records numerados** (duplicaria `destilar-*`). **Rodou 2× (2º giro codebase + 3º giro externo)**: a realização foi **recontagem grelhada no chat**, não glossário-na-nota — confirmada como evidência real, mas numa forma diferente da desenhada (ver frente "Aprender → virar skill").

Régua que costura: **a ferramenta entrega o material, Pedro produz a nota** — onde o output é estado interno, Claude anda em volta e não entrega (igual Investigar e Expor). **O mapa = a nota no NotebookLM** *(colapsado 2026-06-03)*: não há artefato de mapa separado — a nota-compêndio já é a sombra inspecionável que o mapa nomeava. Some o entregável extra, fica o motivo. *"Forma segue o material"* vira **orientação, não obrigação**: a nota (prosa) é o default barato; se o aprendido for fortemente **relacional** (hierarquia de classes, modelo de domínio), externalizar em Excalidraw em vez de/além da nota. Borda **mapa→Expor**: a condição load-bearing é **Claude conseguir extrair a nota** (`notebooklm history`/`download`), não ter arquivo no repo. Simetria notada: fontes+instruções espelha o *briefing* do Investigar (prep pra ferramenta externa → bruto → consolidação no artefato do bloco).

Mapa visual: `assets/metodo-gerencia-v2.excalidraw` (abrir em excalidraw.com → "Open") — altitude "topo magro": 5 blocos + Capturar (ambiente) + Navegador (entrada), conectados por contratos de artefato; esteiras internas ficam só como texto aqui. O `assets/metodo-gerencia-v1.excalidraw` (Expor ainda tracejado, Aprender sem saída definida) e o `assets/fluxo-aprendizado-v1.excalidraw` (esteira sequencial) ficam como registro do "antes".

## Skills vivas

As skills são a implementação dos blocos do método (ver acima). Um bloco com ≥2 skills vira plugin.

**Regra de criação** *(2026-05-29)*: uma skill destila um processo **já exercitado na mão**, não um desenho no papel. Antes de empacotar um fluxo, rodá-lo ad-hoc ao menos uma vez (Claude faz andaime sem skill) e ver onde racha — as skills atuais nasceram assim.

**Regra de instalação** *(2026-06-13)*: skill nova nasce **symlinkada** em `~/.claude/skills/` — senão não auto-carrega e a próxima sessão real que precisar dela racha (visto no `expor`: criado 06-11, symlink adiado, fricção colhida 06-13). Não deixar a decisão de symlinkar "pendente".

- **`destilar-projeto`** *(bloco **Destilar**, destino `projeto`)* — lê docs do projeto + transcrição da sessão (priorizando marcadores) + código sob demanda; propõe atualizações em CLAUDE.md local, README.md, CONTEXT.md. Pedro cura, Claude escreve.
- **`destilar-transversal`** *(bloco **Destilar**, destino `transversal`)* — irmã da de projeto para tudo **fora** de um repositório: CLAUDE.md global, `memory/`, CONTEXT.md global, Notion. Roteia cada fato pra camada certa sem duplicar. A regra de corte (lei seca no global; memória ancora a lei e dá só Why+How; perfil rico na memória; gordo no Notion) vive na própria skill — por isso não está repetida aqui. *(emenda implementada 2026-06-12: ganhou o `~/claude-system/inbox/` como fonte além da sessão atual — drena os candidatos capturados pelo hook do Capturar e apaga o processado; passo 2b da SKILL.md.)*
- **`investigar`** *(bloco **Investigar**, primeiro pedaço)* — entrevista uma ideia/intenção de pesquisa no estilo `grill-me`, uma pergunta por vez, e entrega um *briefing* pesquisável. Faz dois trabalhos numa conversa só: entende o contexto (origem/intenção) e grelha os ângulos até ficarem específicos — o contexto é a bitola contra a qual a pesquisa é grelhada. Redireciona pro **Aprender** quando a intenção é **entender/ganhar capacidade** (não decidir) — sempre situado, a serviço de um projeto ou de uni; curiosidade solta não é mais destino legítimo *(2026-06-19)*. Cobre a formulação; a consolidação em `research.md` é a skill irmã `consolidar-pesquisa` (abaixo).
- **`consolidar-pesquisa`** *(bloco **Investigar**, segunda metade)* — consome o briefing (da `investigar`) + o dump bruto da pesquisa externa, ambos colados no chat, e os consolida no `research.md` (`.claude/research/<slug>.md`). É o que entrega o "artefato de pesquisa" do contrato (linha 30) aos consumidores Aprender e Construir — enquanto o briefing era só interface pra ferramenta externa, não pros consumidores. Régua: o *conteúdo* nunca é do Claude (irmã da regra de corte "a decisão nunca é do Claude"); dump não coberto vira lacuna explícita na §5, não se preenche de cabeça. Forma detalhada do `research.md` (5 seções, fronteira §2 descritivo / §3 decisório, regra de corte em §3+§5) é dona da própria SKILL.md.
- **`expor`** *(bloco **Expor**, criada 2026-06-11)* — motor que vira destilado interno (âncora) + jornada bruta (transcrição/marcadores) em post público, pela esteira `Dossiê → Arco → Escrita+crítica`. A voz é de Pedro: o Claude entrega a 1ª prosa como **andaime substituível** e **critica sem reescrever**. O design load-bearing — as 3 réguas (voz é de Pedro / conteúdo nunca é do Claude / andaime pega na voz, não no fato), o "apresenta a evidência mas pergunta o encadeamento" do Dossiê, e as 7 rachas das 2 rodadas viradas em régua de crítica (molde-não-frase, vigiar o achatamento sem reescrever, autoridade aterrada vs. flutuante ≠ pronome, âncora no caso real, ordenação bottom-up) — vive na própria SKILL.md, não repetido aqui. Escreve no **Notion** (sob ✍️ Expor), nunca no repo. Pendência herdada: plataforma do blog (ver Frentes abertas). **Infra:** **symlinkada 2026-06-13** (`ln -s ~/claude-system/skills/expor ~/.claude/skills/expor`) → auto-carrega a partir da próxima sessão. Gatilho da decisão = **fricção em sessão real** (Pedro precisou do `/expor` no `first-real-project` e não tinha) — o mesmo gap que o hook de captura flagou (ver Frentes abertas, linha do 1º giro live). Virou a "Regra de instalação" acima.
- **`mattpocock/skills`** (instaladas globalmente via `npx skills@latest add mattpocock/skills`) — preenchem a **esteira interna da Construir** (ver linha 41), um estágio por skill:
  - **Idea** → `grill-me` · **Prototype** → `prototype` · **PRD** → `to-prd` · **Kanban** → `to-issues` · **Implementation** → `tdd`
  - **QA** → `improve-codebase-architecture` (deepening de arquitetura) somado às built-in do Claude Code (`code-review`, `verify`, `simplify`, `security-review`)
  - **Research** → vazio de propósito: delega ao bloco **Investigar** (skills `investigar` + `consolidar-pesquisa`)
  - `zoom-out` é transversal (não-estágio). `/setup-matt-pocock-skills` roda uma vez por projeto antes de usá-las (ainda pendente no protótipo dele).
- **`aprender`** *(bloco **Aprender**, criada 2026-06-12; escopo redefinido pra **sempre situado** 2026-06-19)* — conduz Pedro a entender um tema **a serviço de um projeto ou de uni** (nunca curiosidade solta; recebe o redirect do `investigar` quando a intenção é entender/ganhar capacidade, não decidir). Esteira `fontes → diagnóstico do formato → instruções → NotebookLM entrega material → recontagem-grelhada peça-a-peça → nota consolidada`. O design load-bearing — bloco-sink (output intangível precisa de sombra inspecionável), as 2 réguas (a ferramenta entrega o material/Pedro produz a nota; comprimir certo *é* a evidência, grelhada contra as **fontes**, nunca contra o conhecimento do Claude), a forma **realizada** (recontagem-grelhada no chat + nota num `.md` local, **não** o glossário-na-nota-no-NotebookLM que o desenho previa e nunca rodou, n=2), e as 3 rachas do 3º giro viradas em régua (curadoria: fonte densa sequestra o material; portão de instruções checa **cobertura da intenção**; material denso → Claude re-explica simples) — vive na própria SKILL.md, não repetido aqui. **Empacota a forma realizada, aposenta a desenhada.**
- **`navegador`** *(ponto-de-entrada, criada 2026-06-03)* — **não é um bloco**, é o roteador que decide *qual* bloco chamar. Resolve o custo da composabilidade (linha 17): quando Pedro tem a vontade mas não sabe por onde começar. Carrega método (`~/.claude/CONTEXT.md`) + estado local (cwd) cruzados, abre com a **pergunta de camada** (conteúdo → devolve pro trabalho · método → opera · rodar-pelo-método → roteia), grelha a próxima jogada e sugere bloco/skill mostrando qual contrato disparou. O design destilado (grelho load-bearing, regra de carregamento, roteamento) vive na própria SKILL.md — não repetido aqui.
- **Convenção `docs/adr/`** *(decidido 2026-05-29)* — adotada **por-projeto**: ADRs vivem em `docs/adr/` dentro de cada repo de app, alimentando a `improve-codebase-architecture` ao lado do `CONTEXT.md`. **Não no `.claude/` global** — aqui o `CONTEXT.md` já faz o meta-registro. Ativa no primeiro projeto real.
  - **Instalar sem quebrar:** sempre `--global --copy --agent claude-code` e uma flag `--skill` por skill (vírgula não é aceita). Rodar `npx skills add` de *dentro* de `.claude` sem `-g` cria um `.claude/.claude/skills` aninhado; `--copy` (não symlink) garante que migram copiando a pasta.

## Frentes abertas

- **Curadoria dos aprendizados do `health-tech-discovery`** *(✅ concluída 06-22; era o ▶ RETOMAR de 06-20)* — Pedro trouxe os **aprendizados do `health-tech-discovery`** um a um pra cristalizar em valores / redirecionamentos de método (régua: separar *aprendizado* ≠ *valor* ≠ *redirecionamento*; grelhar antes de cristalizar; o conteúdo é do Pedro). **1º fechado** (commit `17e84aa`, 06-19): Aprender vira **"sempre situado"** + `~/dev/learning` eliminado. **2º fechado** (06-20): **construir o produto não basta — falta o território de falar com gente real (validação primária ativa)**; afiou a frente "Validação primária ativa…" abaixo de n=1 a n=2, com a dor concreta + o desenho discovery↔venda + role-split. **3º (06-20): perfil ≠ marca ≠ linha editorial.** O que condiciona tom-do-Expor / lente-do-Investigar / modo-do-Construir é o **perfil** (contexto transversal: quem é, interesses, direção), **não** a **marca** (output público — *emerge* do perfil + output consistente, curada a jusante, linha 59) nem a **linha editorial por-destino** (já homed, linha 104). Decisão: **construir o perfil agora, aterrado na evidência** (3 projetos + valores emergindo), não aspiracional; a marca espera o output existir. O perfil é o **veículo** pra fechar os 2 valores latentes ((b) abaixo + solo-founder). Camada-destino: `user_profile.md` + "Quem é Pedro" no `CLAUDE.md` global (hoje só *dinâmica de trabalho*; falta *interesses / ponto de vista / direção*). **Perfil grelhado e FECHADO 06-20** (estilo `grill-me`, aterrado na evidência, não inventado) → gravado em `CLAUDE.md` global ("Quem é Pedro") + `user_profile.md` (bloco "Interesses e direção"). O que emergiu: **direção** = autonomia de **solo-founder alavancado por AI**, ideia de negócio = veículo intercambiável, competência portável conquistada criando negócios reais; **fundamentos de CC profundos** = base instrumental (faculdade não é credencial nem portfólio); **domínio** exploratório (cluster saúde = genuíno + acesso, **não cristalizado** como o fio — amostra enviesada); **critério** = paixão × alavancagem ("não ingênuo"); **postura de exposição** = produzir nasce público (founder, não empregado), rejeita portfólio-currículo, raiz do Expor — mas é *postura, não meta de volume*. **Pendências:** (a) ✅ **esgotado** (06-22, Pedro: "nem tenho mais"); (b) ✅ **FECHADO 06-22** — o valor *"tomar decisões mais duras/restritas pra alcançar resultado"* teve o **porquê de fundo destilado: verticalização** (a forma é sempre vertical; restringir produto → tarefas → identidade é como busca resultado, não dispersar). O nicho **saúde está decidido** (deliberado, não acesso fácil), mas em **duas camadas que não se colapsam**: *estratégia* decidida → lente do Investigar = saúde; *identidade/autoridade* em construção → não exponível ainda, tom do Expor = jornada (CC/empreender/vendas), não domínio-saúde. Refinou a lei "veículo intercambiável" → intercambiável *dentro* da lane vertical-saúde (opção i). Gravado: `CLAUDE.md` global ("Quem é Pedro") + `user_profile.md` ("Interesses e direção"). **Próximo: (c).** (c) ✅ **registrado 06-22** como frente própria (ver "Marca-com-intenção…" abaixo) — Pedro: só registrar a intenção, não escopar. Frente de projeto viva em paralelo (outro cwd): o **fake-door** + a **instrução de recon** do `health-tech-discovery` (Construir, `~/dev/projects/health-tech-discovery`).
- **Marca-com-intenção + curadoria de ambiente** *(intenção registrada 2026-06-22; **NÃO escopar**)* — Pedro quer agir: perfil-âncora honesto + seguir/consumir/networking com intenção nas redes (**não** produção de conteúdo em massa). Distinção que segura a frente: **input/ambiente** (seguir, consumir, networking) = legítimo já; **face pública/marca + seguidores** = emerge do output a jusante, não se pré-fabrica (régua perfil≠marca). Decisão explícita de Pedro (06-22): **só registrar a intenção, não desenhar solução** — "escopar agora seria desenhar solução pra problema que nem existe" (instância de "complexidade é sinal de erro" + do valor recém-fechado: verticalizar/restringir). Vira frente/projeto próprio **só quando o problema aparecer**.
- **Bloco Destilar → plugin** — com a `destilar-transversal` criada (2026-05-29), o bloco **Destilar** passou a ter 2 skills → é o primeiro candidato a virar *plugin* pelo método. **Não criar agora:** plugin local exige registrar marketplace local em `plugins/known_marketplaces.json` / `installed_plugins.json` com **caminhos absolutos** (`/home/pedro/...`) — estado frágil; skills soltas em `skills/` migram copiando a pasta, sem perda funcional. Quando retomar: `/plugin marketplace add <dir-local>` + `/plugin install`, agrupando as duas skills `destilar-*`.
- **`destilar-projeto` — 2 modos? (curadoria vs. delegação)** *(observado 2026-06-20, `health-tech-discovery`)* — o contrato é "Pedro cura → Claude propõe → Pedro aprova → Claude escreve", mas rodou "Pedro autoriza às cegas → Claude escreve → Pedro vê o resultado" (Pedro postava no FB enquanto delegava a destilação). **Funcionou** porque o conteúdo era reflexo fiel de decisões que Pedro acabou de tomar em voz alta, não inferência. Hipótese: a skill tem **2 variantes legítimas** — curadoria (conteúdo ambíguo/editorial) vs. delegação mecânica (registro do que Pedro acabou de decidir). **Visto 1×; não fixar até repetir** (regra de evolução: exercitar antes de cristalizar).
- **Bloco Construir → plugin** *(decidido 2026-05-29)* — a esteira agora tem 5 skills de estágio (`grill-me`, `prototype`, `to-prd`, `to-issues`, `tdd`). **Tensão a resolver na criação:** o método pede ≥2 skills *próprias* pra virar plugin, mas essas 5 são todas mattpocock (third-party copiadas) — agrupar third-party num plugin "seu" é caso que o método ainda não cobre. A QA mistura built-in do Claude Code (não-empacotáveis) + `improve-codebase-architecture`.
- **Borda Investigar→Construir: o `research.md` incha (duplo-papel)** *(achado 2026-06-06, 1º giro completo da esteira Construir no first-real-project)* — quando Pedro chega com **vontade vaga** (o caso comum, ver CLAUDE.md global), o Investigar vira a **rampa** que transforma a vontade em algo concreto até o PRD, e o `research.md` é o **artefato-gancho** que segura a ideia. Funcionou — mas o `research.md` veio inchado: acumulou **dois papéis no mesmo arquivo**, o gancho exploratório (amplo, "fisga a ideia inteira") e a spec enxuta (entrada do PRD), e o exploratório não se apaga quando vira spec. **n=1.** Possível conserto (a confirmar no próximo giro de ideia-vaga): um passo de **enxugar o gancho** antes de virar entrada da Construir — seja endurecendo a régua de corte do `consolidar-pesquisa` (separar o exploratório do que de fato alimenta o PRD), seja como movimento próprio na borda. Não cristalizar ainda — rodar mais uma vez e ver se o duplo-papel reaparece. **2º giro 2026-06-11 (`ai-scribe-clinico-br`, ideia de negócio em healthcare, frio): o duplo-papel NÃO reapareceu** — o `research.md` saiu enxuto. Hipótese refinada do *porquê* (n=2): **o `research.md` incha quando é a *pesquisa externa* que carrega o papel exploratório ("fisga a ideia inteira"); não incha quando o *Investigar* já afiou a ideia antes e a pesquisa entra só pra decidir (validar/matar).** No `first-real-project` a pesquisa era a rampa exploratória; aqui o Investigar afiou a vontade vaga (healthcare genérico → AI scribe clínico/privado) *antes* de pesquisar, e a pesquisa voltou decisória — com veredito e lacunas, sem gancho amplo a apagar. Se a hipótese aguentar num 3º giro, o conserto deixa de ser "enxugar o gancho no `consolidar-pesquisa`" e passa a ser **garantir que o Investigar afie o suficiente antes de delegar a pesquisa** — o inchaço é sintoma de pesquisar cedo demais, não de consolidar mal. **3º giro 2026-06-17 (`health-tech-discovery`, busca exploratória de mercado pra *gerar* alvo, sem ideia formada): a hipótese AGUENTA (n=3) e ganha nuance.** Era o caso que faltava — panorama-pra-gerar-alvo (não ideia já afiada), o modo que historicamente incha. O `research.md` saiu **enxuto** mesmo assim, porque o Investigar deu **estrutura/bitola** antes de pesquisar (grade de 4 atores × 5 dimensões + filtro de atacabilidade), não um gancho amplo. Refina: o que evita o inchaço não é "ter alvo formado", é o Investigar entregar **recorte** — mesmo que o alvo só emerja da pesquisa. Confirma o conserto "garantir que o Investigar afie antes de delegar".
- **Implementar o Expor** — o bloco foi **desenhado 2026-05-29** (papéis + esteira `Dossiê → Arco → Escrita+crítica`, ver "Método de gerência"). Duas pendências — a 1ª (virar skill) ✅ **fechada 2026-06-11**:
  - **Virar skill** — **rodou na mão pela primeira vez em 2026-05-30** (projeto SAEP, post "Construí um sistema sem entender o problema" — artefatos no Notion: [Expor](https://www.notion.so/370ab645e3bb8125abbbf8df517a76ca) → [post](https://www.notion.so/370ab645e3bb816b830bde6af1301465), movidos de `peri-care-companion/docs/expor/` em 2026-05-30 pra fora do repo público). O gatilho da **Regra de criação** está cumprido: a esteira girou inteira (Dossiê→Arco→Escrita+crítica→post) e duas rachaduras já foram destiladas pra cima (o andaime na Escrita; o "perguntar o encadeamento" no Dossiê). **Não** bloqueado pela migração (skill solta em `skills/` migra copiando a pasta, ver frente do plugin Destilar). **Decidido 2026-05-30: rodar mais uma vez antes de empacotar** — n=1, e a 1ª rodada *virou* um contrato central (o "Claude não entrega prosa" → andaime), então o contrato mais novo tem só um contato. A 2ª rodada deve ser num **tema diferente** e testar: (a) se o contrato do andaime aguenta; (b) se o movimento Dossiê funciona com **âncora bagunçada** (a do SAEP teve dois enunciados de problema datados, espinha fácil — pode ter sido sorte); (c) se roda **frio** (a 1ª foi com Claude de contexto profundo no projeto). Só depois destilar em skill. **2ª rodada iniciada 2026-06-10** (tema: estruturas de dados em TypeScript, âncora no `first-real-project`; gênero A; frio-ish) — produto e esteira no Notion ([sub-página sob ✍️ Expor](https://www.notion.so/37bab645e3bb81dd9284f1202fd9eb50)). Status: **Escrita+crítica CONCLUÍDA 2026-06-11** — corpo revisado; parágrafos de voz/reflexão recontados pelo Pedro, os mecânicos/factuais mantidos do andaime (bate com a racha 3); post fechado, falta só publicar (frente plataforma). Veredito **"o andaime aguenta?" → FECHADO 2026-06-11: aguentou nas 2 sessões.** Molde-não-frase + recusar reescrever a tese mesmo sob pedido mantiveram a voz do Pedro; o risco real (Pedro *achatar pro genérico* ao editar por cima — racha 4) tem **remédio confirmado**: apontar o achatamento como *vigia, sem reescrever*, e ele restaura sozinho (hoje restaurou "montanha" e "reconheci" pela própria mão). Rachas já colhidas: (1) **a lição mudou no meio da Escrita** — escrever revelou que o ponto real era outro (de "a barreira é a sintaxe, não o conceito" → "a estrutura de dados é a pergunta que você decidiu responder rápido", via custo); o *pensar escrevendo* loopou de volta ao Arco, e o contrato aguentou (Claude re-propôs sem sequestrar a voz). (2) **a fronteira gênero A/B é load-bearing na escrita, não só no Dossiê** — fechar no conceito empurra pro B (explainer genérico); a **âncora no caso real** (problema → calendário → traduzir dia em número) é o que segura o A. Costura a vigiar: cada ajuste de altitude (mais conceito vs. mais jornada) move a peça nesse eixo. **Sessão da Escrita da abertura (2026-06-10), três rachas novas:** (3) **o andaime pega menos no factual, mais na voz** — o parágrafo que só descreve o problema (calendário) passou quase intacto e tudo bem; a voz/jornada mora nos parágrafos de reflexão. O andaime "sequestra" onde há voz, não onde há fato. (4) **as edições do Pedro REMOVEM voz, não adicionam** (achado contra-intuitivo) — ao editar por cima do andaime onde "soa natural", ele achatou pro genérico (trocou "instantâneo/travado" por "muito lento"; "ninguém me disse na primeira aula" → "é necessário que você compreenda", que *manda no leitor* = gênero B). O risco do andaime não é só impor a voz do Claude — é o Pedro *neutralizar* a voz que o andaime tinha, deslizando pro B justo na frase-tese. O que destravou na crítica: dar o **molde** (sujeito = Pedro descobrindo, não leitor instruído: *"foi só quando... que eu entendi"*), **não** a frase pronta — e recusar reescrever a frase-tese mesmo quando pedido, pra não sequestrar a voz no ponto que a 2ª rodada testa. (5) **decisão de conteúdo — o anzol da abertura virou capacidade/autonomia**: de "sabendo quase nada você está perto" (proximidade) → "com fundamentos você é capaz de desenvolver soluções" (autonomia), alinhado ao eixo do destino blog-CC; descartado o gancho "TS+AI" como anzol-mercado (perecível, e põe holofote na linguagem que a tese pede pra ignorar), AI ficou só como **espelho** ("até a AI é feita das mesmas estruturas") — honesto e não-perecível. **Sessão da Escrita-corpo (2026-06-11), 2 achados novos:** (6) **régua corrigida — "você" ≠ gênero B.** Blog direciona o leitor de propósito; o eixo não é o pronome, é se a autoridade é **aterrada** (conduz pela experiência ou pelo mecanismo) ou **flutuante** (decreta regra do nada). Claude vinha carimbando *você=B*, o Pedro derrubou — a régua ficou mais fina e é load-bearing pra futura skill: a crítica mira a lição flutuante, não o pronome. (7) **ordenação bottom-up é movimento de ofício da Escrita, não só do Dossiê** — pôr o código **antes** do parágrafo de reconhecimento (mostra o artefato, depois reflete) consertou referência órfã, deu lastro ao espelho da AI e matou eco; gênero A quer evidência-antes-de-reflexão também no sequenciamento dos parágrafos. Disciplinas irmãs: numa fôrma repetida ("Foi resolvendo X") o spot mais forte fica com ela e os outros cedem; e não gastar o takeaway da tese antes da hora (§3 fecha no espelho, não na capacidade — que já é o anzol). **2ª rodada concluída + andaime validado 2× ⇒ skill `expor` criada 2026-06-11** (ver "Skills vivas"): as 7 rachas viraram régua de crítica na SKILL.md. Frente "Virar skill" ✅ **fechada** — resta só a plataforma do blog (abaixo). **3ª passada — adensamento (2026-06-13).** A esteira rodou *de novo* sobre o post já CONCLUÍDO, **reabrindo o Dossiê** pra reforçar a tese existente (custo) com um 2º exemplo real do mesmo código (`Set`, membership "contém?"), + um **diagrama do custo** antes da tese; a frase-tese evoluiu de "Foi resolvendo o calendário…" pra "Foi fazendo a mesma pergunta duas vezes…" (abraça os dois exemplos). **Padrão novo: "adensar" é um modo de reentrada da esteira** — reabrir o Dossiê pra *aprofundar a mesma tese*, distinto da 1ª escrita (que *descobre* a tese); Pedro escolheu reforçar (`Set`) em vez de abrir movimento novo (`reduce`/tipo-soma ficaram fora de propósito). **Réguas aguentaram pela 3ª vez** (andaime substituível + molde-não-frase: dei "Foi [gatilho] que eu entendi", Pedro preencheu o gatilho — emergente, criou a escalada duas→um-milhão). **Régua nova destilada pra SKILL.md:** fonte visual reforça gênero A se serve a tese, tabela de referência é gênero B (ver "Skills vivas → expor").
  - **Plataforma do blog** — onde o output público vive. Implementação pura; decidir quando for publicar de fato. **Modelo decidido 2026-06-10:** o **Expor fica agnóstico (motor); a linha editorial vive num projeto-destino separado.** O primeiro destino é um **blog pra estudantes de CC** (eixo: fundamentos de algoritmos/CC escondidos na jornada técnica real — gênero A bottom-up), e é o 1º de N destinos (cada um com público/eixo próprio). A peça nova que o destino adiciona é **dados, não código**: um *critério de curadoria de tema* no movimento Dossiê (*"este destilado toca um fundamento de CC ensinável a um estudante?"*) que entra no Expor como parâmetro de contexto. Risco vigiado: o destino **define a linha editorial e hospeda**, não "gera" conteúdo — quem gera é o motor; se o destino virar gerador, duplica o motor.
- **Aprender → virou skill em 2026-06-12** *(`aprender`, ver "Skills vivas")* ✅ **FECHADA** — gatilho "rodar com fonte externa antes de empacotar" cumprido no 3º giro (2026-06-11); a skill empacota a **forma realizada** (recontagem-grelhada no chat + nota num `.md` local), aposentando a desenhada (glossário-na-nota-no-NotebookLM, n=2 sem rodar). As 2 réguas + a esteira + as 3 rachas do 3º giro viraram a SKILL.md. **1º contato como skill — ✅ rodou e aguentou (2026-06-12).** Pedro invocou `/aprender` no `organograma-enfermagem-hmsvp` (tema deploy/banco/Vercel/Supabase; num `~/dev/*` **sem `.claude/` local** — o cenário que morderia o furo per-cwd). Resultado (relato do Pedro: "foi tudo perfeito"): (a) **forma empacotada aguentou** — recontagem-grelhada + nota num `.md` local (`nota-deploy-banco.md`, 4 peças nas palavras dele); (b) **portão de instruções** — o material que a skill instruiu o NotebookLM a gerar e o grelho saíram **no nível ideal**, sem estreitamento reportado; (c) **furo per-cwd — não mordeu neste giro** (o tema não exigiu perfil/memória rica; o essencial carrega via CLAUDE.md global). Cuidado: isso é "não foi exigido", **não** "resolvido" — a skill ainda não carrega `memory/` por caminho absoluto como o navegador, decisão de design aberta pra quando o tema pedir contexto rico. **n=1 como skill, primeiro sucesso** — vigiar a sustentação nos próximos contatos. **Contato 2026-06-18 (`health-tech-discovery`, fonte = `research.md` já destilado):** a válvula "diagnóstico do formato" resolveu pra **pular o NotebookLM** (síntese-sobre-síntese seria redundante) — recontagem-grelhada direto, com o `research.md` como bitola. 1ª vez que a válvula escolhe "sem ferramenta" e aguenta: confirma que a régua "forma segue o material" cobre também *não gerar material novo* quando a entrada já é densa e auditável. Histórico dos 3 giros (na mão) abaixo. — **a esteira já rodou na mão**: UML pra destravar a Etapa 2 do `health-plan-authorization` (2026-06-03; uma rodada anterior foi apagada na migração antes de destilar). O gatilho "rodar uma vez" disparou. **NotebookLM instalado em 2026-06-02** via `notebooklm-py` (CLI + skill em `~/.claude/skills/notebooklm/`; autenticado; `uv` em `~/.local/bin`) — infra resolvida. **Não empacotar ainda:** o desenho mudou bastante hoje (movimento "diagnóstico do formato"; colapso **mapa = nota no NotebookLM**) e esse desenho refinado só teve esse contato. Espelhando a decisão do Expor, rodar mais uma vez num **tema diferente** — testando se o colapso mapa=nota aguenta, se a borda mapa→Expor (extrair a nota via `history`/`download`) funciona de fato, e — **novo (2026-06-09, da skill `teach`)** — se a **camada de evidência** (Pedro comprime conceitos nas próprias palavras, Claude grelha contra as fontes) destrava capacidade real ou só adiciona cerimônia — antes de destilar em skill. Forma atual da esteira: ver "Esteira interna do Aprender". **2º giro iniciado 2026-06-09** (tema: entender o `first-real-project` por dentro, peça por peça; frio; ver CONTEXT local do projeto). Entrada nova exercitada: **fonte = o próprio repo** (antes era assunto externo). Rachas que essa entrada expôs, já colhidas: (1) **o NotebookLM rejeita código** (`.ts/.tsx` → 400; só `.md` entra); (2) **subir código privado a serviço externo bate no classificador de exfiltração do harness** — exige o usuário autorizar na mão (rodou via `!`); juntas, dizem que pra codebase a esteira hoje só engole os *docs*, não o código. (3) **o material da ferramenta não é automaticamente didático** — o report do NotebookLM veio acadêmico/denso e sobrecarregou o Pedro ("não entendo nada"); destravou quando o Claude re-explicou simples, com exemplo concreto do domínio. Tensiona a régua "a ferramenta entrega o material, Pedro produz a nota": material denso demais trava o sink e o Claude precisa intervir (bate com o CLAUDE.md global — quando Pedro trava, intervir). (4) **lição de entrega:** despejar o vocabulário do método (movimentos, "camada de evidência", "grelhar") no aprendiz sobrecarrega — o método é andaime invisível. **2º giro CONCLUÍDO 2026-06-10** — as 6 peças do trajeto recontadas por Pedro com as próprias palavras (detalhe peça-a-peça no CONTEXT local do projeto). Veredito da iteração: (a) **A camada de evidência se paga — mas se realizou numa forma diferente da desenhada.** O desenho previa Pedro comprimir conceitos num glossário *na nota do NotebookLM*, Claude grelhar contra as fontes. O que rodou: **recontagem conversacional no chat**, peça a peça — Claude explica, Pedro reconta nas próprias palavras, Claude grelha a recontagem **contra o código real do repo** (cada peça ancorada no arquivo). Funcionou: destravou entendimento real (Pedro corrigiu deslizes sozinho quando apertado). Confirma o núcleo do teach (*comprimir certo é a evidência*) e o grelho-contra-as-fontes — mas a **realização foi oral/iterativa no chat, não glossário-na-nota**. Hipótese a carregar: com codebase como fonte, a evidência mora na recontagem grelhada, não numa nota-compêndio. (b) **Com codebase como fonte, o NotebookLM fica marginal** (fecha as rachas #1/#3): rejeita código e o material vem denso — o **motor real do aprendizado foi Claude+Pedro no chat ancorado no código**, NotebookLM só de apoio. Reforça a tensão da régua "a ferramenta entrega o material": pra esse tipo de fonte a ferramenta entrega *pouco*, o andaime do Claude carrega o peso. (c) **Lição de entrega nova:** Pedro tende a responder *"por quê"* repetindo *"o quê"* (resposta circular); destrava apertar pelo **contraste** (o que a peça **não** faz). Soma à lição (4). **Ainda não cristalizar a skill:** este giro exercitou **codebase** (entrada específica); o colapso mapa=nota e a forma glossário-na-nota **não** foram testados (a evidência veio pelo chat). Falta um giro com **fonte externa** rodando a forma desenhada antes de empacotar. **3º giro enfileirado (intenção declarada 2026-06-11):** Pedro vai rodar a esteira do Aprender no `research.md` do `ai-scribe-clinico-br` — fonte **externa** (saúde/regulação), exatamente o caso que falta, e o NotebookLM volta a ser central (ao contrário do 2º giro, codebase, onde ficou marginal). Cuidado herdado: a bibliografia [1]–[42] do research **não foi colada**, então a régua "grelhar contra as fontes" fica frouxa se a única fonte for o próprio research (síntese não-auditada) — decidir no passo 1 (seleção de fontes) se recupera as fontes primárias, o que **também fecharia o buraco de proveniência** que o próprio research aponta na §5. Distinção a segurar no giro: o Aprender **equipa** a decisão seguir/parar/B-C-D (handoff do Investigar), não a **toma**. **3º giro CONCLUÍDO 2026-06-11** (o enfileirado acima rodou: território do AI scribe clínico BR; **fonte externa**, frio; entrada = `research.md`). No passo 1 a decisão de **recuperar fontes primárias** foi tomada — 4 URLs públicas oficiais (Anvisa SaMD, ANS TISS, CFM CID-11, + explainer de SOAP) aterraram os conceitos, **fechando parcialmente o buraco de proveniência da §5**. Achados: **(a) a forma desenhada rachou de novo, do mesmo jeito — n=2.** O design pedia glossário-na-nota *dentro do NotebookLM*; rodou como **Claude escreve a recompressão do Pedro num `.md` local (`nota-territorio.md`) + grelho conversacional no chat**, peça a peça — idêntico ao 2º giro (codebase). Com n=2 (codebase + externa) a deviação deixa de ser acidente da fonte: **a nota quer viver fora do NotebookLM; a evidência mora na recontagem grelhada, não num glossário digitado lá.** E refuta a previsão do enfileiramento ("NotebookLM volta a ser central"): mesmo com fonte externa, o NotebookLM **entregou o material mas não foi o motor do aprendizado** — a camada de evidência rodou no chat de novo. Conserto a cristalizar: a consolidação = Claude registra as definições tight *nas palavras do Pedro* num arquivo, grelhando cada uma **contra as fontes do passo 1** — o NotebookLM entrega material, não hospeda a nota. **(b) A camada de evidência pagou, agora com fonte externa** (antes só codebase): Pedro se corrigiu sozinho sob pressão (glosa, CID-11), transferiu a régua "de onde vem o conteúdo" entre conceitos sem reentrega, e unificou 4 conceitos numa lei só. Confirma o núcleo do teach (*comprimir certo é a evidência*) — sobe de hipótese pra **confirmada na realização recontagem-grelhada** (a forma glossário-na-nota-no-NotebookLM segue não-testada; n=2 sugere abandoná-la). **Gatilho "rodar com fonte externa" cumprido** — falta decidir se cristaliza a skill com a forma *realizada* (recontagem-grelhada), não a desenhada. **3 rachas novas deste giro:** (5) **subir o `research.md` privado ao NotebookLM NÃO bateu no classificador de exfiltração** (racha #2 do 2º giro não reproduziu — entrou direto, sem `!`); a hipótese atual: o classificador distingue *código-fonte privado* de *doc de pesquisa em prosa*. (6) **uma fonte densa sequestra o material** — o PDF Q&A da Anvisa dominou o peso e o study guide enviesou pra regulação (puxou pro caminho B que não fora escolhido); achado sobre *curadoria de fontes* no passo 1, não sobre o prompt. (7) **o passo de instruções estreita a intenção em silêncio** — Pedro pediu "conceitos **+ mapa de atores**", o Claude escopou só conceitos e o portão "Pedro aprova antes de ir" **não pegou** porque o estreitamento estava *dentro* do prompt aprovado. É o risco que o design do bloco já nomeia ("o Claude molda em silêncio *o que* se aprende") se materializando — sinaliza que o portão precisa checar *cobertura da intenção*, não só aprovar o texto. **Costura reaberta — escopo: aprendizado *situado* (2026-06-15)** *(via inbox, `health-plan-authorization`)*: a skill declara escopo "curiosidade pura, sem ação/decisão por trás" (redirect do Investigar), mas rodou num caso com **ação atrás** (decisões de modelagem reais do projeto). Resolução na sessão: **importar a mecânica** (régua NotebookLM + grelhar-contra-fonte) **sem chamar de uso canônico** — fronteira nomeada e cruzada de propósito. **✅ RESOLVIDA 2026-06-19 por decisão de valor do Pedro (antecipa o n=3):** o escopo da `aprender` **amplia** — o bloco passa a ser **sempre situado** (a serviço de `projects` ou `uni`); **curiosidade pura é aposentada como movimento legítimo**, não nasce variante. Motivo (valor): todo aprendizado do Pedro é *by doing*, sob demanda — "ou faculdade, ou construir pra gerar valor"; o que sempre houver pra aprender se filtra exigindo um **mestre** (projeto/cadeira). A fronteira Investigar↔Aprender deixa de ser *curiosidade-vs-ação* e vira **entender-vs-decidir** (ambos situados). Coerência dupla: o diretório `~/dev/learning` morre junto (vira `{projects,uni}`) — filtro de pasta e filtro de método viram a mesma regra. Bônus de design: situar **fortalece** a camada de evidência do bloco-sink (a própria construção/decisão atrás já é parte da sombra). Propagado nesta sessão: `SKILL.md` da `aprender` (escopo + fronteira), redirect do Investigar, definição do bloco acima, memória `user_profile` (2 pastas). **Não tocado de propósito:** `CLAUDE.md` global — o valor mais largo ("decisões mais duras e restritas") fica pra destilar quando o Pedro abrir o porquê.
- **Navegador → virou skill em 2026-06-03** *(`navegador`, ver "Skills vivas")* — gatilho da linha 63 cumprido: rodou na mão 2x em 2026-05-30 (`fase1` + `health-plan-authorization`, independentes), **rachou as duas vezes na mesma fronteira método↔projeto** (grelhava na camada de conteúdo sem captar a intenção meta). O que as rodadas destilaram — pergunta de camada, carregar os dois docs cruzados, grelho load-bearing — virou a SKILL.md (não repetido aqui). **Pendência: primeiro contato como skill.** Nunca rodou *como* skill, só na mão; o teste real é a próxima vez que Pedro precisar do navegador num projeto — usar a skill e ver se o empacotamento aguenta ou racha em lugar novo. **Furo a vigiar nesse contato:** a memória é *per-cwd* (só carrega quando o cwd é `~/claude-system`) — num projeto em `~/dev/*`, perfil/feedback globais não carregam, e o navegador roda justamente em projetos variados. Não resolvido; decisão de design aberta. **1º contato como skill ✓ (2026-06-06, `first-real-project`):** rodou *como* skill pela primeira vez, num projeto em `~/dev/*`. Roteou certo (pergunta de camada → método → "colher" → `destilar-transversal`) e o empacotamento aguentou — mas **as duas fronteiras que o navegador previu apareceram no handoff de saída**: (1) a skill seguinte (`destilar-transversal`) lê a *sessão atual*, mas a fonte do aprendizado era o histórico do projeto, não a conversa — o navegador entregou apontando a fonte certa, contornando na mão; (2) o navegador roteia a skill mas **não dimensiona o escopo** do que ela vai fazer, deixando o handoff frouxo. Padrão emergente: o navegador acerta *qual* bloco, mas o *contrato de entrada* da skill destino pode não bater com o que a jogada real exige — costura a vigiar nos próximos contatos. Sobre o furo per-cwd ✅ **resolvido 2026-06-11:** o conserto é o navegador **carregar o `memory/` por caminho absoluto** (`~/claude-system/memory/`, simétrico ao jeito que já carrega o CONTEXT), de qualquer cwd — sem depender do auto-load. Diagnóstico que encolheu o furo: o essencial (leis secas + quem é Pedro) **sempre** carrega via `~/.claude/CLAUDE.md`, independe de cwd; o `memory/` só adiciona o enriquecimento (estrutura `~/dev/{projects,uni}`, Why+How das leis), então o navegador nunca ficava cego em `~/dev/*`, só sem afinação. A memória per-cwd **em si** segue como **limitação de plataforma** (o Claude Code computa o path pelo cwd-slug; não dá pra fazer a global "sempre carregar" sem symlinkar por-projeto, que misturaria memória de projeto com a global) — documentada, não consertável aqui; o que se consertou foi a **dependência do navegador** nela. **Generalização (2026-06-11):** o furo não é só da memória — o próprio `CONTEXT.md` (onde vivem as definições das esteiras dos blocos) **também não auto-carrega** em `~/dev/*`; só a migalha do CLAUDE.md global ("ler `~/.claude/CONTEXT.md` antes de agir no sistema de orquestração") aponta pra ele. O essencial (leis secas + quem é Pedro) sempre carrega via CLAUDE.md global; o `CONTEXT.md` é um terceiro nível, só *apontado*. O navegador resolveu pra si carregando por caminho absoluto, mas pra um **bloco rodado na mão** (ex.: Aprender, que ainda não é skill) num projeto sem `.claude/` local, **não há conserto automático** — a instrução do Pedro precisa ser explícita ("lê `~/.claude/CONTEXT.md` e roda a esteira X"), senão depende do Claude obedecer a migalha por conta própria. Verificado no `ai-scribe-clinico-br`: pasta sem `CLAUDE.md` nem `.claude/` locais. **Contato como skill 2026-06-15** (`health-plan-authorization`, roteou uma revisão de alinhamento): a régua load-bearing aguentou — a jogada (revisar o emparelhamento) **emergiu da fala do Pedro**, não foi cravada. Duas costuras: (1) a **pergunta de camada pegou a aparente** ("conteúdo"), a **real** ("método") só apareceu ao grelhar o *porquê* ("conferir antes de investir, sem atrito no aprendizado") — reforça (n+1) a racha de 06-13 "falta o passo qual-problema-dói antes de grelhar a jogada"; (2) roteou pra um **trabalho sem skill destino** (a frente "Revisar projetos ativos" não é skill), reexpondo o handoff-frouxo-sem-skill já conhecido. **Racha 2026-06-17** (`health-plan-authorization`, abertura da implementação): a régua "camada conteúdo → devolve e sai" tem **falso-negativo** — a Fatia 0 (scaffold) era cerimônia do assistente por contrato (PRD §8), não conteúdo do Pedro. A skill flagou sozinha em "Onde rachou". **Conserto na SKILL.md:** branch "Conteúdo" virou dois sub-casos (conteúdo do Pedro = sai; cerimônia do assistente = executa). **Contato 2026-06-17 (`claude-system`, gênese de ideia de negócio):** roteou um caso **pré-projeto** — Pedro sem ideia, querendo *gerar/escolher* o quê fazer. A pergunta de camada não casou com as 3 (conteúdo/método/rodar): a jogada vivia **antes** delas, na borda de cima da Gênese ("gerar a ideia"). O navegador nomeou isso e roteou certo (→ Investigar). Borda a vigiar: a taxonomia de camadas não cobre o "pré-projeto/geração de ideia" — apareceu 1×, não cristalizar. **Contato 2026-06-20 (`health-tech-discovery`):** o navegador **pulou a pergunta de camada** declarando-a "óbvia" (estado local inequívoco apontava "fazer o conteúdo rodar pelo método") — e repetiu o atalho **2× no mesmo movimento** (na abertura e na `destilar-transversal` seguinte). Sinal recorrente (a SKILL.md põe a pergunta como *"sempre, a primeira"*, e foi pulá-la que rachou as 2 rodadas na mão). **Deliberar:** o "sempre" admite critério de dispensa quando o estado crava a camada, ou a literalidade protege contra o *falso-óbvio* (Pedro com intenção meta não detectada)? **Não mexer na SKILL.md até decidir.**
- **Revisar projetos ativos compatibilizados com o método** *(em andamento desde 2026-05-30)* — Pedro aplicou o prompt-navegador acima nos projetos ativos pra torná-los compatíveis com os blocos; o Claude faz a **revisão/feedback** do estado de cada um contra o método. Entrada: Pedro aponta os projetos. **Como buscar as decisões num projeto:** o *commit de adoção do método* (msg tipo "adopt method blocks") + os docs destilados (CLAUDE/CONTEXT/COLABORACAO locais) vencem as transcrições `.jsonl` cruas — é o destilado que precisa ser lido. **Revisados e ajustados (commits no próprio repo):** `fase1`, `health-plan-authorization`. Pendentes: os que Pedro ainda apontar. **Lente da revisão** *(2026-06-15)*: revisar contra **fricção-no-aprendizado, não compliance** — o critério não é "o projeto está conforme o mapa?" e sim "o método estorva o objetivo do projeto?". Barateia (foca o caminho crítico, não varre doc-a-doc) e muda o veredito. **2ª revisão do `health-plan-authorization` (2026-06-15)** com essa lente: **alinhado, nada a ajustar** — o esqueleto que ele usa (esteira da Construir) é a parte do método estável desde a 1ª revisão (06-03); o que evoluiu depois (Aprender/Expor virando skill, Gênese, captura) ou não toca o projeto ou ele já antecipou na prática (a `COLABORACAO.md §1` já fazia o "intercalar construir+aprender" que o método só destilou em 06-12). Distinção colhida no caminho: a régua do **Aprender-skill** "Claude grelha contra as fontes, nunca contra o próprio conhecimento" **não vale** num projeto cujo conteúdo é canônico (POO/Java/UML) — lá o Claude É autoridade técnica legítima, oposto do pressuposto da skill; não importar a régua por reflexo.
- **Skills hardcodam caminhos de raiz que projetos sobrescrevem** *(achado 2026-05-30; ✅ resolvido 2026-06-09)* — `consolidar-pesquisa` escrevia em `.claude/research/<slug>.md`; mas projetos reais põem esses artefatos em `docs/` por natureza (ex.: `health-plan`: `docs/pesquisa/`). **Fix:** a `consolidar-pesquisa` agora descobre a convenção do projeto (docs de pesquisa existentes ou CLAUDE/CONTEXT local) e confirma o caminho com Pedro, com `.claude/research/` só como default. **Correção do achado original:** a outra metade ("`to-prd` escreve o PRD na raiz") não procedia — o `to-prd` (mattpocock) **publica no issue tracker**, não escreve arquivo no repo. Logo foi um fix de uma skill, não duas. A instância por-projeto fica no CONTEXT local do projeto.
- **Caminhos Windows obsoletos nas skills** *(achado 2026-06-03; ✅ resolvido 2026-06-09)* — a `destilar-transversal` hardcodava `C:\Users\pedro\.claude\...` na tabela de camadas (3 linhas) → trocado por `~/.claude/...`. Varredura `grep -rn 'C:\' --include='*.md'` no repo inteiro confirmou que era o único hardcode de comportamento; as menções restantes a `C:\` são descritivas (frentes do CONTEXT, memória de migração).
- **De onde o PRD do `first-real-project` foi parar em `docs/`?** *(achado 2026-06-09; ✅ resolvido 2026-06-10)* — premissa dissolvida: **nenhuma skill da esteira escreveu o PRD**. O `PRD.md` nasceu no scaffold do projeto (commit `1e17860 chore: esqueleto do projeto`), e os issues `docs/issues/01-10` vieram de um passo manual ("fatia PRD em 10 issues") que manteve o Kanban como **arquivos locais** em vez de publicar em tracker — escolha do projeto, não bug de skill. Não há caminho hardcoded a consertar; é o layout de docs do repo. Caminho real é `docs/`, não `docs/construcao/` (a frente tinha o path errado).
- **Sincronia hub↔projeto↔sessões** *(desafio aberto, levantado 2026-06-12; sem solução)* — a Gênese (ver "Gênese de projeto") manda o trabalho pra dentro do repo do projeto, mas o hub (claude-system + Claude + Construções) precisa ficar *current*, e Pedro hoje sustenta isso **rodando destilar repetidamente** — ritual manual que **drifta no instante que ele esquece**. Dois eixos: (a) **intra-projeto** — sessões paralelas não compartilham estado *in-flight* (ver memória `feedback-parallel-sessions`); o que compartilham é o **repo**, então git commit é o ponto de sync e a latência é entre "sessão A aprendeu X" e "X foi commitado"; (b) **projeto↔hub** — o destilar (docs locais) + a Construção são o ponto de sync, a latência é a frequência do destilar. **Reframe (hipótese 2026-06-12):** os dois eixos são **o mesmo problema — frescor de artefato.** Sessões/hub não trocam estado vivo, trocam *artefatos* (linha 21); sync = quão fresco está o artefato, e o gargalo é o **custo de escrever** (commit/destilar são atos explícitos; entre atos, o conhecimento vive só no contexto de uma sessão, invisível ao resto). Consequência: a cura durável **não** é "destilar mais vezes na mão" (escala mal, drifta) e sim **baratear/automatizar a escrita** pra encolher a latência — é o que o "Capturar → hook" (linha 25) já aponta, ainda não implementado. Até lá, o hub é **current-as-of-último-destilar**, e isso tem que ser **aceito como staleness conhecido**, não fingido. Não cristalizar mecanismo ainda — primeiro decidir se o reframe (um problema, não dois) aguenta. **RESOLVIDO EM PARTE (2026-06-12, via `grill-me`):** o reframe foi **refutado** — são **dois** problemas, não um. **(A) transporte projeto→hub:** o feedback de método rodado em `~/dev/*` se perde porque *nada o sinaliza no fluxo* (diagnóstico de Pedro: "nada sinalizou" — não foi marcador que falhou, foi não-haver-momento-de-parar-e-marcar) = frescor/custo-de-escrita; cura = captura automática barata. **(B) intercalar construir+aprender numa sessão** (olhar a prática enquanto se aprende o conceito por trás): **não é frescor**, é acoplamento de fios — e o valor (co-presença) é o *oposto* de paralelismo, então NÃO pede infra de sincronia entre sessões. B é **hábito, não mecanismo** ("conduzir bem a sessão: construo uma fatia, explico, construo a próxima"); o trabalho de design é todo no A. Distinção load-bearing: *"blocos autônomos"* é sobre **ordem** (compõem sem sequência fixa), intercalar é sobre **co-presença temporal** — eixos diferentes, não confundir. **Desenho fechado do A — captura barata projeto→hub:** hook **`SessionEnd`** em qualquer projeto → Claude **headless** (`claude -p`) lê a transcrição da sessão → se achou feedback-de-método, escreve uma **nota-candidato rica** (2-4 linhas) em **`~/claude-system/inbox/<timestamp>-<projeto>.md`** (um arquivo *por sessão*, caminho absoluto, sem race entre sessões paralelas); se não achou, **não escreve nada** (auto-filtro → inbox só acumula sinal). Roda *fora/depois* da sessão de trabalho → não atrapalha o intercalar do B (restrição herdada). **Drenagem manual:** Pedro roda `/destilar-transversal`, que ganha o `inbox/` como fonte, propõe as destilações e arquiva/apaga o processado. A divisão que segura tudo: **captura automática (à prova do "nada sinalizou") desacoplada de destilação humana (decisão/conteúdo nunca é do Claude)** — o inbox é o buffer entre as duas; "esquecer de destilar" passa a só *atrasar*, não *perder*. **Duas emendas de contrato (regra de evolução = *preencher*, não quebrar):** (1) **Capturar** deixa de "Claude não captura ativamente" → captura candidatos no fim de sessão (o diagnóstico provou que o contrato antigo perdia o que passa no fluxo); (2) **destilar-transversal** ganha o `inbox/` como fonte além da sessão atual. **CONSTRUÍDO 2026-06-12** (sessão remota): `hooks/capturar-feedback-metodo.sh` + `hooks/capturar-prompt.md` + `inbox/` + emenda na `destilar-transversal` (passo 2b) + bloco `hooks` no `settings.json` versionado. Riscos do desenho resolvidos no build: `SessionEnd` **passa** `transcript_path`/`cwd`/`reason` (confirmado na doc oficial; detalhe: doc do evento ainda incompleta, issue #6306); recursão (o headless também dispara `SessionEnd`) cortada por env var `CAPTURAR_HEADLESS` herdada; `reason=resume` pulado (a transcrição continua — capturaria duplicado); sessões com cwd no próprio hub puladas. Encanamento testado com stub (candidato→escreve; sem→não; hub/resume/recursão→saem). **ATIVADO 2026-06-13** (sessão local): o build veio do container remoto via patch → `git am` → push SSH (o 403 do remoto era a credencial de integração dele, não a chave local; daqui empurrou liso); bloco `hooks` colado no `~/.claude/settings.json` ativo (cópia, não symlink — não propaga do repo), JSON validado, `jq`+`claude` confirmados no PATH. **Revisão de papel antes do giro (2026-06-13):** o `.sh` e o prompt foram grelhados. Veredito: hook sólido (timeout 300, anti-recursão, resume/hub/transcript-ausente todos cobertos; medido num transcript real do `first-real-project` — 1.2MB, 497 linhas, 160 delas >2000 chars → é majoritariamente ruído de tool-result, não conversa). Furo achado: o `capturar-prompt.md` não dizia *onde* olhar. **Emenda escrita (bloco "Como ler o transcript"): foca nos turnos de conversa user/assistant, trata tool-result como ruído, e auto-contém o "abra com Read".** Ataca os dois eixos que o giro mede (qualidade da nota + custo). **A emenda está NO arquivo mas NÃO commitada de propósito** — commitar junto com a observação do 1º giro. **1º giro INICIADO 2026-06-13** (sessão de trabalho no `organograma-enfermagem-hmsvp`, instância limpa em `~/dev/*` mantida ingênua ao teste pra não contaminar a transcrição). **Ao voltar, julgar a nota do `inbox/`:** (a) latência/custo no fim-de-sessão; (b) apareceu nota?; (c) qualidade — pegou método e não conteúdo de projeto, contexto sobrevive dias, citou o sinal certo, volume conservador (0-2). Se nota vazia + havia sinal → distinguir auto-filtro de quebra pelo `--debug`. Depois commitar emenda + observação, fechando o eixo A. **1º giro JULGADO 2026-06-13: o hook nunca rodou — bug de encanamento no `claude -p`, consertado.** Inbox vazio era quebra silenciosa (causa b), não auto-filtro (causa a) — indistinguíveis sem reproduzir na mão (o `|| exit 0` engolia o erro → exit 0 limpo). Dois bugs na mesma chamada: (1) `input=$(cat)` drena o stdin do hook e o `claude -p` herda o fd, travando em "no stdin data"; (2) no CLI 2.1.160 prompt grande no **argumento posicional** não é lido (`Error: Input must be provided`). Conserto único: **passar o prompt por stdin via pipe próprio**, não como argumento. Pós-conserto, ponta a ponta com `SessionEnd(reason=clear)` real: nota escrita, 84s (sonnet), guardas intactas (resume/hub/recursão → exit 0 sem nota). Qualidade (eixo c): pegou **método não-conteúdo** (capturou a variante "aprender sem NotebookLM, fonte = `git log` do projeto"; ignorou o trabalho de PDF/logo), contexto sobrevive, volume 1 — só vazou um preâmbulo apesar do "sem preâmbulo" (cosmético, Pedro drena na mão). **Lição de método:** o `|| exit 0` que protege contra ruído também **mascara quebra** — um hook silencioso por design precisa de um modo de auto-teste (ex.: `CAPTURAR_DEBUG` que loga o erro do `claude`), senão "vazio" é ambíguo. **✅ implementado 2026-06-13:** `CAPTURAR_DEBUG` setado faz o hook logar cada ponto de decisão (early-exits + exit code/stderr do `claude -p` + qual dos 3 desfechos: `QUEBRA`/`auto-filtro`/`candidato escrito`) em `hooks/capturar-debug.log` (gitignored, por-máquina); `dbg()` é no-op sem a var. Testado: os 4 early-exits logam certo e o no-off não escreve nada; worker ponta-a-ponta com `claude -p` real logou `auto-filtro: SEM_CANDIDATO` (papo de UI corretamente rejeitado como não-método). Agora "inbox vazio" vem acompanhado da linha que distingue auto-filtro de quebra — desambigua os falsos-negativos que confundiram os 2 giros live. **Eixo A validado por reprodução manual (JSON alimentado na mão); falta um giro *live*** — o hook disparando sozinho no fim de uma sessão real em `~/dev/*` — pra fechar de fato. **Onde o navegador rachou nesta sessão** (pra destilar a skill depois): na camada=método, ele só convergiu *depois* de errar o alvo (foco nos "regimes") e Pedro mandar reagir ao próprio desenho — faltou um passo "qual problema **dói**?" antes de grelhar a jogada (separar problema elegante de problema presente cedo). **1º giro LIVE 2026-06-13 (volta ao `first-real-project`): o hook DISPAROU sozinho — mas o headless morreu antes do veredito; nova quebra, distinta do bug do stdin.** A sessão real (3ª passada do Expor) fechou → 2 `SessionEnd` → 2 headless concorrentes (confirma o disparo live que faltava). Ambos morreram aos ~34s sem emitir veredito (último evento `ai-title`, inbox vazio). Causa-raiz, **dois fatores somados**: (1) o `Read` recusa transcript >256KB (o real tinha **812KB**) → o modelo improvisava com `Bash` (apesar do `--allowedTools Read`, que **não conteve**) → lento; (2) o filho do hook é **morto no teardown da sessão** aos ~34s, antes de decidir. Inbox-vazio aqui era de novo **quebra-(b) disfarçada de auto-filtro-(a)** — a mesma ambiguidade que o `CAPTURAR_DEBUG` não-feito mascara. **Conserto (validado standalone, ainda não live):** (a) **pré-filtro `jq` no shell** extrai só prosa `user`/`assistant` e descarta tool-result/thinking/tool_use (**812KB→44KB**), embutida no prompt — mata o cap do Read e o improviso de Bash de uma vez; (b) **`setsid`** destaca o `claude -p`+escrita num process group próprio, imune ao teardown (worker escreveu nota em **78s**, hook retorna exit 0 *na hora* → também para de bloquear o fim de sessão). Prompt atualizado: seção "A conversa da sessão" (conversa vem embutida, não mais "abra com Read"). **Qualidade (eixo c):** candidatos legítimos **método-não-conteúdo** (régua de titulação ausente no Expor; gap do symlink da `/expor`). **Mas variância (n=3 reproduções: nada / régua-titulação / gap-symlink)** — o veredito é **não-determinístico**, falso-negativo possível numa sessão *densa* de sinal; risco de qualidade a vigiar, **não consertado**. **Ainda falta o giro live de verdade com o conserto** (hook disparando sozinho no fim de uma sessão real, não reprodução na mão) pra confirmar que o `setsid` sobrevive ao teardown no campo — e pra ver se a variância morde. **Veredito do giro live (2026-06-17, verificado):** separar as duas metades. **(mecânica) ✅ CONFIRMADA** — 4 notas drenadas eram **untracked** (sem história no git, conteúdo de sessões reais do `health-plan-authorization`, a de hoje batendo com a racha do navegador da abertura da implementação) ⇒ não foram reprodução manual, o **`setsid` sobreviveu ao teardown no campo ≥4×**; qualidade método-não-conteúdo nas 4. **(confiabilidade) ❌ SEGUE ABERTA** — as 4 são **4 sessões distintas, captura única cada**, então **não testam variância de re-run** (a dúvida da frente era re-rodar a *mesma* sessão densa); e o `CAPTURAR_DEBUG` estava **off**, então os **falsos-negativos ficaram invisíveis** (só vi true-positives, taxa de perda não-medida). **Ação tomada:** `CAPTURAR_DEBUG=1` ligado no `~/.claude/settings.json` ativo (bloco `env`) — a partir de agora cada sessão loga decisão (incl. filtra/quebra) em `hooks/capturar-debug.log`, tirando os falsos-negativos da invisibilidade. Só depois disso dá pra julgar variância com dado; até lá o eixo A é **mecânica-fechada, confiabilidade-em-observação**.
- **Validação primária ativa não tem bloco** *(observado 2026-06-18, `health-tech-discovery`)* — o mapa cobre **desk** (Investigar: fontes secundárias **e** escuta social / reclamação não-solicitada — ambas *sem interação*) e **teste de demanda** (Construir/Prototype: fake-door). Mas **conversar/entrevistar usuário real** — validação primária *ativa* — não tem casa. Nesta sessão **roteou-se em volta**: o WTP do autônomo de saúde foi pro fake-door (que é Construir), então o gap **não chegou a travar** — n=1, fraco. Não cristalizar; vigiar se reaparece quando um discovery precisar de entrevista que o fake-door não substitui. Refina de passagem o **Investigar**: a escuta social é um *modo* dele (primária não-solicitada, observação), não bloco novo. **Afiado 2026-06-20 (n=2, dor concreta — 2º aprendizado da curadoria health-tech):** a fenda saiu de latente — a dor viva de Pedro é **buscar contatos + entender a dor verdadeira** indo atrás de gente real. Quatro peças destiladas (rodar na mão antes de cristalizar — regra de criação):
  - **(1) Investigar tem hoje 2 modos** (desk + escuta passiva/observação); falta o **3º: primária ativa** — você *provoca* a interação (pergunta, aborda, entrevista). Nenhuma fonte secundária entrega.
  - **(2) Discovery e venda = dois atos de intenção oposta costurados pelo contato como artefato de borda** — nem uma casa só, nem duas sem relação. O **contato** é o artefato compartilhado (nasce na descoberta, *gradua* pra venda). A régua que separa os lados é o **Mom Test** (Rob Fitzpatrick): na descoberta **não se vende** — pergunta sobre o passado/comportamento real ("como você cobra hoje? quanto perdeu mês passado?"), nunca o futuro hipotético ("você usaria?"), senão a gentileza contamina o sinal. Fork em aberto (de Pedro, decidir rodando): **(a)** descoberta-ativa = *modo novo do Investigar* + vender = *bloco novo* [Claude defende — mantém a fronteira Mom Test estrutural, não reabre o Investigar como faz-tudo]; **(b)** um bloco único "falar com gente" com a regra dentro [menos peças, mistura intenções opostas].
  - **(3) Role-split análogo ao Investigar** (Claude prepara briefing → Pedro executa o meio insubstituível → Claude consolida) — mais evidência pro caminho (a). Mas **vaza em 3 pontos**: (i) a **fonte reage** → a instrução é um *molde de perguntas* que sobrevive ao desvio, não um briefing linear; (ii) o **artefato acumula** (lista de contatos = estado de relação, cresce pra sempre) ≠ `research.md` one-shot → `consolidar-pesquisa` cru não serve; (iii) **"organizar" = grelhar elogio vs. dor real** (*"eu compraria"* = ruído; *"hoje pago R$X numa gambiarra"* = sinal) — **o ponto mais perigoso**, é o **bloco-sink do Aprender um nível acima** (colher cobertura ≠ entender a dor; sentir-se validado ≠ validar).
  - **(4) Ferramenta de busca web = recon, não a fenda.** Pedro quer que o Claude escreva a instrução pra uma ferramenta externa alcançar domínios que o Claude não toca (ex.: grupo de Facebook) e poupar energia. Realismo: a ferramenta entrega **escuta passiva acelerada** (posts que já existem; grupo *fechado* costuma barrá-la também → o membro é Pedro) — **escala o modo que o mapa já tem, não a primária ativa** (nenhuma ferramenta faz o follow-up pro fisio). Risco: poupar energia *pulando a parte insubstituível* → validação aparente. Enquadramento: **ferramenta = recon** (onde estão / quem reclama / com que palavras), **Pedro = a conversa ativa**; o recon **afia** a conversa, não a substitui. **Formato de retorno quando a fonte tem login wall** (Reddit/Instagram/apps fechados, que barram tanto o Claude quanto a ferramenta de busca): Pedro traz **dump bruto de HTML** ou **screenshot**, Claude filtra/processa. Validado 06-20 (`health-tech-discovery`: HTML do r/PsicologiaBR + screenshot da resposta no FB, ambos limpos) — fecha o gap tácito da régua "garimpo é do Pedro" (que cobre *quem coleta*, não *como a evidência volta*).

  **Disciplina:** ainda **não cristalizar bloco** — rodar na mão no health-tech (atrás dos primeiros fisios) e ver os 3 vazamentos na pele primeiro. **Escopo mais largo latente** (não destilado ainda): a fenda é instância de um reframe maior — *construir o produto não basta; vender/relacionamento e sustentar o negócio também não têm casa no mapa (hoje produto+conhecimento)*; o valor por trás é **solo-founder alavancado por AI cobrindo funções de muitos funcionários** (intenção fundadora, agora sentida). Primária-ativa é só a parte que **dói agora**.
- **Outras peças da orquestração** que Pedro guardou para trazer depois.
- **Página Notion do mapa** — ✅ criada 2026-05-31: [Método de gerência — blocos componíveis](https://www.notion.so/370ab645e3bb816f9e75d7db2d241b5b), sub-página de "Sistema de gestão", aponta pro `metodo-gerencia-v2.excalidraw`. Pendência residual: embutir o desenho na própria página (hoje só linka o arquivo local).
- **Análise retrospectiva do `history.jsonl`** — 1.129 prompts em `C:\Users\pedro\.claude\history.jsonl` (06/04/2026 → presente; troca de SO em ~2026-06-02, histórico anterior perdido). Adiado até o sistema mínimo girar. Quando retomar: copiar pra fora do `.claude/` (editar in-place quebra a up-arrow), definir critérios de filtro juntos, analisar em ondas.

## Construções vivas no Notion

Página raiz: [Sistema de gestão](https://www.notion.so/36eab645e3bb8160969adc118f4bf38d) · database [Construções](https://www.notion.so/0fc713f696674fe3ace03860d22231b3).

- **[Mapeamento bagunçado de ideias de negócio](https://www.notion.so/36eab645e3bb81c7bd18c48860e01b06)** — rascunho. Próxima sessão: brainstorm livre.
- **`ai-scribe-clinico-br`** *(fase Investigar, 2026-06-11)* — ideia de negócio: startup de AI scribe clínico (texto/fala do médico → prontuário + CID) pro setor privado BR. Bloco Investigar girou inteiro → `research.md` consolidado (veredito: tese **generalista** difícil — terreno ocupado por PEPs com IA + registro Anvisa; 3 caminhos estreitos abertos: nicho de especialidade, PEP-leve, ou faturamento/glosa B2B). Repo em `~/dev/projects/ai-scribe-clinico-br` (`research.md` + `CONTEXT.md` locais). Conecta com "Mapeamento bagunçado de ideias de negócio" acima. Entrada no database [Construções](https://www.notion.so/37dab645e3bb81178b48f906788f81e4) criada 2026-06-12 (status `ativo`).
- **[first-real-project — relatórios de frequência](https://www.notion.so/376ab645e3bb81f6935fd72700848367)** — ✅ concluído (2026-06-10). Produto web, primeiro end-to-end do Pedro, no ar na Vercel (10/10 slices). Esteira Construir girou **inteira**: PRD → Kanban (10 issues em `docs/issues/`) → implementação → QA. Repo em `~/dev/projects/first-real-project`.
- **[organograma-enfermagem-hmsvp](https://www.notion.so/37dab645e3bb8159b7adcbb1951aa040)** — ✅ núcleo concluído (2026-06-12). **2º produto end-to-end** do Pedro, no ar (Vercel + Supabase). Organograma da enfermagem do HMSVP, feito originalmente pelo pai no Claude chat; este projeto deu **persistência (fonte única no Supabase) + cadeado** (só o pai escreve, login email+senha, RLS travada no UID). **Ângulo:** o projeto onde o Pedro fez o **procedural consciente** (deploy/banco/auth na mão) — contraponto deliberado à "esteira no automático" do `first-real-project`. O `/aprender` deploy/banco **rodou** (2026-06-12, nota-sombra `nota-deploy-banco.md`): recontagem grelhada das 4 peças contra o briefing, produtiva — o insight da "fonte única justifica o backend" casou com o motivo do passo 3. **Possível 1º contato do bloco Aprender como *skill*** (ver frente Aprender). **Sessão 2026-06-13:** fechadas as frentes **PDF em folha única** (causa-raiz: bloco `body.printing` era código morto perdido no truncamento; conserto = religar via `beforeprint`/`afterprint` + `zoom` medido pra caber em A3, A4 removido), **logo oficial do hospital no cabeçalho** (embutida em base64) e **editar a árvore** (✅ decidido: sem editor visual — frequência de mudança é rara, então Pedro edita o array `org` no código e dá push; guia de 4 operações no comentário; mina aceita: renomear/mover um setor zera seus números, o `_id` deriva do título). **Núcleo + todas as frentes resolvidas.** Repo privado em `~/dev/projects/organograma-enfermagem-hmsvp`.
- **[health-tech-discovery](https://www.notion.so/382ab645e3bb81db8ae0c33ae995c0bf)** *(Investigar→Construir, 2026-06-18)* — discovery de ideia de negócio: processo administrativo de pequeno estabelecimento de saúde no BR pra enxugar→automatizar como **dev solo**. É um **funil**. 1º giro: grade de 4 atores → afunilou o **autônomo de saúde**. **2º giro (06-18, `research-autonomo-saude.md`):** gancho vertical = **Receita Saúde + cobrança + caixa mínimo**; persona **fisio/psico/dentista** (nutri fora — consulta não-dedutível no IRPF, logo sem Receita Saúde); repasse e LGPD caíram. **Decisão (06-18):** ir pro **Construir** testando demanda antes de codar — **fake-door** (landing + preço + captura de email) em `prototype/fake-door/` mede o WTP, o único elo não-validado pela desk+escuta. Risco aberto: **API da Receita Saúde não verificada** (recibo automático encenado). Repo em `~/dev/projects/health-tech-discovery`. Conecta com "Mapeamento bagunçado de ideias de negócio" acima. Status `ativo`.
